using System;
using System.Collections.Generic;
using System.Text;
using Engine;
using Microsoft.Xna.Framework;

class CameraPosition : TickTick
{

    public CameraPosition()
    {
    }

    


    /*cameraPosition = new Vector2((int)Player.position.X - (windowSize.X / 2), (int)Player.position.Y - 60 - (windowSize.Y / 2));
    if (cameraPosition.X < 0)
        cameraPosition.X = 0;
    if (cameraPosition.Y < 0)
        cameraPosition.Y = 0;
    if (cameraPosition.X > worldSize.X - windowSize.X)
        cameraPosition.X = worldSize.X - windowSize.X;
    if (cameraPosition.Y > worldSize.Y - windowSize.Y)
        cameraPosition.Y = worldSize.Y - windowSize.Y;

    HandleInput();
    GameStateManager.Update(gameTime);*/

}

